# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ApodDynamicImages' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ApodDynamicImages

  target 'ApodDynamicImagesTests' do
    inherit! :search_paths
    # Pods for testing
    use_frameworks! # Required for Swift Test Targets only
    inherit! :search_paths # Required for not double-linking libraries in the app and test targets.
    pod 'EarlGrey'
  end

end
